import { Layout } from "@/components/layout/Layout";

export default function Dashboard() {
  return (
    <Layout>
      <div className="container mx-auto px-4 py-20">
        <h1 className="text-3xl font-serif font-bold text-primary mb-6">Investor Dashboard</h1>
        <p className="text-muted-foreground">Live account view placeholder.</p>
      </div>
    </Layout>
  );
}
